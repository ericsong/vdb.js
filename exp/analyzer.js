var tree = {
    data: 'b',
    left: {
        data: {str: 'lol'},
        left: null,
        right: null
    },
    right: {
        data: 'c',
        left: null,
        right: null
    }
}

/*
 * Check that all properties in 'props' array are properties of 'node'. Remove 'props' that are not present in 'node' 
 */
function checkProperties(node, props) {
}

function analyzer(root) {
    var commonProperties = Object.getOwnPropertyNames(root);



}
